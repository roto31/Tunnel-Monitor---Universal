name: Release pkg

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      version:
        description: "Version (e.g. 1.0.0). Defaults to the tag without v prefix."
        required: false

permissions:
  contents: write

jobs:
  build-pkg:
    name: Build & release .pkg
    runs-on: macos-14
    env:
      DEVELOPER_ID_APPLICATION: ${{ secrets.DEVELOPER_ID_APPLICATION }}
      DEVELOPER_ID_INSTALLER:   ${{ secrets.DEVELOPER_ID_INSTALLER }}
      APPLE_ID:                 ${{ secrets.APPLE_ID }}
      APPLE_TEAM_ID:            ${{ secrets.APPLE_TEAM_ID }}
      APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
      MACOS_CERT_P12_BASE64:    ${{ secrets.MACOS_CERT_P12_BASE64 }}
      MACOS_CERT_P12_PASSWORD:  ${{ secrets.MACOS_CERT_P12_PASSWORD }}

    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea3d419bc6fbc2e94d6b # v4.2.2

      - name: Resolve version
        id: ver
        run: |
          if [[ "${GITHUB_REF_TYPE:-}" == "tag" ]]; then
            V="${GITHUB_REF_NAME#v}"
          elif [[ -n "${{ inputs.version }}" ]]; then
            V="${{ inputs.version }}"
          else
            V="0.0.0-dev"
          fi
          echo "version=${V}" >> "${GITHUB_OUTPUT}"
          echo "Resolved version: ${V}"

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode.app

      - name: Import signing cert
        if: env.MACOS_CERT_P12_BASE64 != ''
        run: |
          set -euo pipefail
          CERT="${RUNNER_TEMP}/signing.p12"
          KEYCHAIN="${RUNNER_TEMP}/build.keychain"
          KEYCHAIN_PWD="github-actions"
          echo "${MACOS_CERT_P12_BASE64}" | base64 --decode > "${CERT}"
          security create-keychain -p "${KEYCHAIN_PWD}" "${KEYCHAIN}"
          security set-keychain-settings -lut 21600 "${KEYCHAIN}"
          security unlock-keychain -p "${KEYCHAIN_PWD}" "${KEYCHAIN}"
          security import "${CERT}" -k "${KEYCHAIN}" -P "${MACOS_CERT_P12_PASSWORD}" \
              -T /usr/bin/codesign -T /usr/bin/security -T /usr/bin/productsign
          security set-key-partition-list -S apple-tool:,apple: -s -k "${KEYCHAIN_PWD}" "${KEYCHAIN}"
          security list-keychains -d user -s "${KEYCHAIN}" $(security list-keychains -d user | tr -d '"')

      - name: Build menu bar app
        env:
          APP_SRC: ${{ github.workspace }}/mac/app/TunnelMonitor
          DIST_DIR: ${{ github.workspace }}/build/dist
        run: |
          VERSION="${{ steps.ver.outputs.version }}" bash build/build-app.sh

      - name: Build .pkg
        run: |
          VERSION="${{ steps.ver.outputs.version }}" bash build/build-pkg.sh

      - name: Upload artifact
        uses: actions/upload-artifact@65c88fcd6040d0c04ac32b0487bee3dd4c059fae # v4.6.0
        with:
          name: tunnel-monitor-pkg-${{ steps.ver.outputs.version }}
          path: |
            build/releases/*/Tunnel-Monitor-*.pkg
            build/dist/Tunnel-Monitor-*.pkg

      - name: Create GitHub release
        if: github.ref_type == 'tag'
        uses: softprops/action-gh-release@e7a2fe0680d5a15519aedd8d8ccb62eaa268a500 # v2.9.0
        with:
          files: |
            build/releases/*/Tunnel-Monitor-*.pkg
            build/dist/Tunnel-Monitor-*.pkg
          generate_release_notes: true
