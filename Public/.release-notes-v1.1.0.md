## Tunnel Monitor v1.1.0

### Code

- Liquid Glass menu bar UI (macOS 26+ with Reduce Transparency fallback).
- Automated Liquid Glass app icon (`actool` / Icon Composer pipeline).
- Versioned artifacts under `build/releases/02-v1.1.0/`.

### Data

- `dataRevision`: `wizard-fields` public-v1, `liquid-glass-v1` icon + `Assets.car` (see `datasets/bundle-manifest.json`).

### Install

Download `Tunnel-Monitor-1.1.0.pkg`, open, and follow the setup wizard. See `mac/CHANGELOG.md` and `docs/tunnel-monitor/`.
